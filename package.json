{
  "name": "scalable-rate-limiter",
  "version": "1.0.9",
  "description": "A rate limiter that uses redis to horizontally scale across servers. It allows a user to set the number of tokens per user per timeframe. What separates this package from the rest is it is race-condition safe since it uses a LUA script for atomic funcs",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha.js --reporter spec"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/metricstory/scalable-rate-limiter.git"
  },
  "keywords": [
    "ratelimiter",
    "rate",
    "rate",
    "limiting",
    "rate",
    "limiting",
    "api",
    "throttle",
    "apithrottle",
    "googleanalytics",
    "analytics",
    "google",
    "twitterapi"
  ],
  "author": "will@metricstory.com & brandon@metricstory.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/metricstory/scalable-rate-limiter/issues"
  },
  "homepage": "https://github.com/metricstory/scalable-rate-limiter#readme",
  "dependencies": {
    "moment-timezone": "^0.5.34"
  },
  "devDependencies": {
    "chai": "^4.3.6",
    "mocha": "^10.0.0",
    "redis": "^4.1.0"
  }
}
